// index.js
const svgGenerator = require('./lib/svgGenerator');
const logoGenerator = require('./lib/logoGenerator');

// Uncomment one of the following lines to run either the SVG generator or the logo generator

 svgGenerator(); // Run the SVG generator
// logoGenerator('Your Text', 'blue', 'Triangle', 'red'); // Run the logo generator

